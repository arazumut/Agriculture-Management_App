import 'package:flutter/material.dart';

class AppColors {
  // Ana renkler
  static const Color primary = Color(0xFF2E7D32); // Koyu yeşil
  static const Color primaryLight = Color(0xFF4CAF50); // Açık yeşil
  static const Color primaryDark = Color(0xFF1B5E20); // Çok koyu yeşil

  // İkincil renkler
  static const Color secondary = Color(0xFFFF9800); // Turuncu
  static const Color secondaryLight = Color(0xFFFFB74D); // Açık turuncu
  static const Color secondaryDark = Color(0xFFF57C00); // Koyu turuncu

  // Nötr renkler
  static const Color background = Color(0xFFFAFAFA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color error = Color(0xFFD32F2F);
  static const Color warning = Color(0xFFFFA000);
  static const Color success = Color(0xFF388E3C);
  static const Color info = Color(0xFF1976D2);

  // Metin renkleri
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textDisabled = Color(0xFFBDBDBD);

  // Tarım temalı renkler
  static const Color soil = Color(0xFF8D6E63); // Toprak rengi
  static const Color water = Color(0xFF2196F3); // Su rengi
  static const Color sun = Color(0xFFFFEB3B); // Güneş rengi
  static const Color leaf = Color(0xFF4CAF50); // Yaprak rengi
  static const Color harvest = Color(0xFFFF9800); // Hasat rengi

  // Hayvancılık temalı renkler
  static const Color livestock = Color(0xFF795548); // Hayvan rengi
  static const Color milk = Color(0xFFFAFAFA); // Süt rengi
  static const Color meat = Color(0xFFD32F2F); // Et rengi
  static const Color egg = Color(0xFFFFF9C4); // Yumurta rengi
}
